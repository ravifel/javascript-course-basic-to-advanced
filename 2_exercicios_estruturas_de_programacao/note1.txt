02- Crie uma estrutura IF que verifica a entrada em um evento, se tiver 18 anos ou mais, pode entrar. Armazenar a idade em uma variável com "let". Insira uma instrução "console.log("Pode entrar")", caso a pessoa tenha mais que 18 anos.

03- Crie uma constante com o seu nome e depois verifique se o seu nome é realmente oque está na constante, usando uma estrutura conficional "IF". Caso for igual, emita uma mensagem de saudação com "console.log()".

04- Teste a função "Math.pow()" com o "console.log" utilizando as seguintes bases: 2, 3, 18 e o expoente deve ser o 2.
Observação: A função "Math.pow()" exibe a potencia de um número, recebendo 2 argumentos: a base e o expoente respectivamente. (Exemplo: Math.pow(5,3);)

05- Armazene a velocidade de um carro em uma variável, com o número que desejar. Faça uma estrutura "If/Else" que verifica se ele está a cima da velocidade máxima permitida que é 80. Se estiver acima ou abaixo da velocidade permitida devem ser exibidas mensagens no "console.log()".

06- Faça uma estrutura "If/Else" para verificar se um usuário pode dirigir. Armazene em variáveis diferentes alguma sinformações sobre o usuário (idade e se ele tem CNH).
Se a idade for maior que 18 anos e não possuir CNH, exiba uma mensagem.
Se a idade for maior que 18 e tem CNH, exiba uma mensagem.
Se a idade for inferior a 18 anos e não tiver CNH, exiba outra mensagem.

07- Exibir um loop "While" que exiba números de 0 a 10 no "console.log()".

08- Escreva um loop "For" que exibe números de 100 a 50 no "console.log()".

09- Escreva um loop "For" ou "While" que exiba qual o número é par e qual número é impar. O contador deve iniciar de 0 e ir até 50.

10- Verifique se o número é primo. Um número primo é um número maior que 1 e apenas divisível por sí próprio e por 1.